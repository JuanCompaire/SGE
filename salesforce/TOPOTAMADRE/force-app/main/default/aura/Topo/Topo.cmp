<aura:component>
    <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>
    <aura:attribute type="boolean" name="isTopo"/>
    <span class="topo" aura:id="topo" onclick="{!c.topoClicked}">5</span>
</aura:component>	
